# SOS Scout Assets

This directory contains configuration files and assets specific to the **Scout** edition (Minimal).

## Contents
- `.bashrc`: Custom shell configuration for the live environment.
- `hostname`: Sets the hostname to `sos-scout`.
- `motd`: Message of the day.
